> 历史记录：批量设置界面已被 2026-09-13 入金列表地址轮换流程替代。以下为此前阶段记录，不代表当前入口或 MFA 行为。当前验证见 `docs/verification/2026-09-13-deposit-address-rotation.md`。

# 用户批量收款地址验证记录

## 实现范围

- 用户列表显式勾选、当前页全选/半选、跨页/搜索保留、已选名单移除与清空；抽屉打开时冻结目标 UID。
- 原生币种/网络选择，多行地址；币种关联支持网络，重复组合和地址格式校验。
- 补充、覆盖指定组合、恢复公共地址；预览新增/覆盖/恢复/跳过/不可处理明细，确认后执行。按用户要求无 MFA。
- 逐项成功/失败/跳过/冲突及审计记录；失败/冲突项目重新预览后重试，成功项不重放。
- 单人配置入口、列表摘要和用户详情专属地址展示。公共地址使用共享模拟仓库。
- 专属配置优先解析，无专属配置才继承公共配置；失效专属配置阻止解析；历史订单不变。

## 自动化验证

2026-09-12：`npm test`，591 个测试全部通过；`npm run build` 成功；`git diff --check` 无错误。

新增 7 个领域行为测试和 2 个 Vue SFC 行为测试覆盖：预览无写入、补充/覆盖隔离、无效组合/重复/空集合、专属失效、恢复边界、缺失或变更的公共地址、预览不可变/冲突、幂等、审计失败无写入、仅失败项重试、失效配置重新启用、遮罩关闭抵抗、连续 Escape 不放弃草稿、预览后无 MFA 提交、重复确认防护。

公共地址页面与查询监听的两条旧源码断言已更新到共享仓库与独立分页监听；审计演示记录覆盖新增的两类操作。

## 实际浏览器验证

在本地 Vite + Codex 内置浏览器验证：

- 第一页勾选 user_1001，第二页勾选 user_2001：目标为 2 位，明确显示 1 位不在当前页，表头半选。
- 两条 USDT/ERC20、USDT/TRC20 配置预览产生 4 项新增；确认直接保存 4 项，无 MFA；逐项展示审计 ID。
- 单人再次打开可见两条专属地址；恢复 user_2001 的 TRC20 公共地址预览及执行成功。
- 1440×900 编辑时切换 390×844：两条草稿保留，仅一个抽屉，宽 390px，无横向溢出。
- 1280×720、768×1024、1024×768、844×390、320×568：测量抽屉/关闭按钮/页脚矩形均在视口内、没有抽屉横向溢出、仅一个活动实例；背景 `inert` 保留。此组为结果态几何检查，不代表所有输入态已逐项验证。
- 1280×720：最后按钮 Tab 回到关闭按钮，关闭按钮 Shift+Tab 回到最后按钮；点击遮罩仍有一个抽屉；Escape 关闭后抽屉数 0、焦点回到原收款地址按钮、背景 inert 和 body 滚动锁清除。

## 未验证及接入边界

- 未验证：真实触摸/拖动/滑动、屏幕阅读器、200% 浏览器缩放、系统字体放大/长翻译、虚拟键盘、动态浏览器栏、四向非零 safe area、系统 reduced-motion/高对比度。需在对应真机或浏览器模拟环境中完成编辑、预览、确认和关闭流程。
- 未逐项验证：所有视口下全部编辑与恢复状态、关闭动画中切换视口、路由离开草稿确认、多层叠加及异步网络失败。共享生命周期已有回归测试，本次实际浏览器检查不能替代这些场景。
- 当前为现有项目架构下的内存 Mock 实现，路由切换保留，整页刷新重置；没有持久化后端、真实身份权限校验或链上到账匹配。
- 地址仅做所选网络的基础格式校验，不能证明链上存在、校验和正确、归属或资产兼容性。上线须由服务端验证支持网络、地址、权限/用户资格、请求幂等和配置版本，并将写入与审计放入事务。
- 前台充值演示页面使用独立演示身份与地址数据，未将其强行映射到后台 UID。真实充值端需要接入仓库对应的用户+币种+网络解析接口；历史订单应继续使用创建时的地址快照，到账归属不能只依据共享地址。
- 未提交 Git，保留了用户原有的操作栏宽度调整。
