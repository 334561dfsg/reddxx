# Repository Link Documentation Change

## Goal

Add the canonical GitHub repository URL to the root README so contributors can identify and open the project repository directly.

## Scope

- Update only `README.md` during implementation.
- Add a short `Repository` section linking to `https://github.com/ImEvanQi/fex-admin`.
- Do not change application code, dependencies, configuration, or generated outputs.
- Do not include the existing untracked `outputs/ai-agent-opportunity-map/` directory in any commit.

## Verification

- Review the README diff for the intended link only.
- Confirm the commit contains no unrelated files.
- Push the implementation commit to `origin/main` to verify SSH write access.

