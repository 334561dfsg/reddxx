> 历史记录：批量设置界面已被 2026-09-13 入金列表地址轮换流程替代。以下为此前阶段记录，不代表当前入口或 MFA 行为。当前验证见 `docs/verification/2026-09-13-deposit-address-rotation.md`。

# 用户批量收款地址实现计划

目标：将同一套币种/网络/地址应用到显式选中的用户，支持跨页选择、单人维护、恢复公共配置、影响预览及逐项结果。按用户最新要求，不进行 MFA。

实现边界：当前项目使用模拟仓库，延续该架构。公开地址改为共享仓库实例，专属地址优先解析；不修改历史订单或推断充值归属，不将演示前台身份与后台 UID 强行关联。实际生产鉴权、链上地址校验与到账匹配仍由后端负责。

- [x] 领域仓库：先写 Node 行为测试，覆盖补充/覆盖隔离、合法组合、去重、恢复、预览快照冲突、幂等和部分失败；实现 preview / execute / resolve / list。
- [x] 用户列表：保留现有未提交样式，新增显式跨页选择、当前页全选、选择清单和单人地址摘要；修复筛选分页监听冲突，忽略迟到查询。
- [x] 配置抽屉：原生选择框、多行编辑、预览、确认、结果与仅失败项重试；复用共享模态生命周期；保留草稿、固定头尾、仅正文滚动、四向安全区域。
- [x] 单人详情展示专属地址和公共继承说明，地址变更接入用户审计日志。
- [x] 运行领域/组件测试、全套 Node 测试和生产构建；浏览器验证桌面/窄屏、选择、编辑、覆盖、恢复和焦点，记录未验证范围。

验证结果见 `docs/verification/2026-09-12-user-deposit-address.md`。591 个测试通过，构建成功；未验证的真机行为与后端边界已单独记录。
